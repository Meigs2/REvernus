<Window x:Class="REvernus.Views.SettingsManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:REvernus.ViewModels"
        mc:Ignorable="d"
        Title="Settings Manager" Height="391" Width="508" ResizeMode="NoResize">
    <Window.DataContext>
        <local:SettingsManagerViewModel/>
    </Window.DataContext>
    <Grid>


        <TabControl TabStripPlacement="Left" Grid.ColumnSpan="3">
            <TabItem Header="General Settings">
                <StackPanel Margin="5">
                    <GroupBox Header="Market Order Settings">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,5,0">
                                <Label Content="Auto Update Timer" Margin="5,0,0,5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding AutoUpdateTimer, Mode=TwoWay}" Width="85" HorizontalAlignment="Right" Margin="170,0,0,0" Height="20" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>
            <TabItem Header="Hotkey Settings">
                <GroupBox Header="Market Hotkeys" Grid.ColumnSpan="3" Grid.RowSpan="1">

                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="111*"/>
                            <ColumnDefinition Width="194*"/>
                            <ColumnDefinition Width="96*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="25"/>
                            <RowDefinition/>

                        </Grid.RowDefinitions>

                        <Label Content="Market Up" Grid.Column="0" Grid.Row="0"/>
                        <TextBox Text="{Binding MarketHotkeyUp, Mode=TwoWay}" Grid.Column="2" Grid.Row="0"/>

                        <Label Content="Market Down" Grid.Column="0" Grid.Row="1"/>
                        <TextBox Text="{Binding MarketHotkeyDown, Mode=TwoWay}" Grid.Column="2" Grid.Row="1"/>

                    </Grid>
                </GroupBox>
            </TabItem>
        </TabControl>
    </Grid>
</Window>

