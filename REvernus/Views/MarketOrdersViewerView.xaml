<UserControl x:Class="REvernus.Views.MarketOrdersViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:REvernus.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:MarketOrdersViewerViewModel/>
    </UserControl.DataContext>
    <Grid>
        <DockPanel>
            <Grid x:Name="TopGrid"  DockPanel.Dock="Top" Height="30">
                <Button Command="{Binding GetOrdersCommand}"></Button>
            </Grid>
            <TabControl x:Name="MainTabControl">
                <DataGrid x:Name="SellOrderDataGrid" ItemsSource="{Binding MarketOrders, Mode=OneWay, NotifyOnSourceUpdated=True,UpdateSourceTrigger=PropertyChanged}"
                          CanUserAddRows="False" IsReadOnly="True">
                </DataGrid>
            </TabControl>
        </DockPanel>
    </Grid>
</UserControl>
